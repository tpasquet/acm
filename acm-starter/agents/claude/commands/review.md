Review the staged or unstaged changes in this repository.

Steps:
1. Run `git diff HEAD` to see all changes
2. Check for correctness, edge cases, and security issues
3. Check that the code follows the conventions in @context/instructions.md
4. Report findings grouped by severity: **critical**, **suggestion**, **nit**
5. If nothing is wrong, say so explicitly

Do not fix anything — only report.
